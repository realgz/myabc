# 未留痕债务登记表

（空格两段式确认 + M5 各条追加于此，日期 2026-09-11；置顶方便查看，历史条目见下方原表）

| 日期 | 条目 | 处理状态 |
|---|---|---|
| 2026-09-11 | **空格键智能ABC 风格两段式确认——用户明确表示这是"做这个输入法的唯一目标"，完整决策见 `docs/decisions/input-engine/20260911-space-key-two-step-confirm.md`**。M1 起空格键一直是"无条件选中候选[0]"，用户在 M5 完成后 real-machine 验证前提出这不是他要的智能ABC体验：候选有歧义（>=2 个）时，空格不该一下子把候选[0]拍上屏，应该先"架住"（高亮）等用户确认，第二次空格或直接按数字键才真正选中；候选只有 0/1 个时没有歧义，空格照常直接上屏。经两轮 `AskUserQuestion` 精确到具体按键 transcript 才动工，避免猜错这个用户最看重的行为。实现：`Session` 新增 `space_armed_` 状态位 + `SessionResult::armed_index`；协议新增 `uiShow.armedIndex`（PROTOCOL_VERSION 5->6）；候选窗新增高亮渲染。不区分候选来源（拼音/数字金额统一处理，不做特殊豁免），数字键选字完全不受影响（仍是一步直选）。真实 IPC 端到端验证（含拼音多候选、数字模式、继续打字清空架住态、数字键直选四种场景）+ M4/M5 全部既有回归测试重跑通过。 | 已实现并验证 |

| 日期 | 条目 | 处理状态 |
|---|---|---|
| 2026-09-11 | **M5：不建 `src/domains/user-dict/` 独立领域，不建 `new_phrase_store`/`learning_policy` 的完整策略层**。plan 06 §3.1 设计了一整套自建组件：`user_store`（封装 `pinyin_train`+`pinyin_save`）、`new_phrase_store`（自建拼音->词->次数计数表，"SQLite？否，用 libpinyin addon phrase 或简单二进制"+"DbBackend 适配器"）、`learning_policy`（晋升阈值/衰减策略）。探查 libpinyin API 后发现：`pinyin_remember_user_input(instance, phrase, count)`（第一次见于 `third_party/libpinyin/src/pinyin.h` "Remember the current phrase and pinyin pair"）本身就是一个**按 count 累加的自学习模型**——不管这句话本来存不存在于词库，调一次就记一次/加一次计数，配合已有的 `SORT_BY_FREQUENCY` 候选排序，天然实现"用得越多排得越前、新词也能被记住"，不需要在它之上再叠一层"攒够 N 次才允许创建"的门槛（那需要一份跨重启的 (拼音,词条)->次数 计数存储，纯为一个没有验收判据强制要求的场景加复杂度——3 个以上实现/明确行为分支才触发适配器强制线，这里只有"要不要落盘"这一件事是真需要自建的逻辑）。**决策**：不建 user-dict 领域目录、不建 new_phrase_store/DbBackend 适配器；`RememberUserInput`/`Train` 直接加进 `LibPinyinEngine`（跟 M1 起的 `Choose`/`Reset` 同一个类），"何时训练"的判断内联在 `Session::MaybeTrain`（跟 M1 起`Session::ProcessKey`同样"业务判断在 logic 层"的路数），"多久落盘一次"独立成 `learning_policy.hpp` 的 `AutosaveCounter`（这是唯一真正自建、且值得单测的策略逻辑）。 | 已决策并实现，M5 |
| 2026-09-11 | **M5：真发现——`select_keys`/`current_source_uses_engine_choose_` 之外，"选中即整句" 的 UsesEngineChoose 分流本身跟"训练时机"有耦合，MaybeTrain 必须在 engine_.Reset() 之前调用**。`pinyin_remember_user_input`/`pinyin_train` 读的是当前 `pinyin_instance_t` 的 matrix/nbest 结果，`Session::ResetToIdle()`（进而 `engine_.Reset()`）会清空这些状态——训练调用必须插在"算出最终 commit 文本"和"ResetToIdle()"之间，不能像 M1-M4 的其它收尾逻辑一样放在 `BuildViewResult` 之后统一处理。已在 `Session::SelectCandidate`（done==true 分支）和 `Session::CommitComposition`（`current_source_uses_engine_choose_` 分支）两处正确插入，真实 IPC 端到端验证过训练确实生效（见下一条）。 | 已实现并验证，M5 |
| 2026-09-11 | **M5：造新词 + 频率提升的真实验证结果（诚实记录，同 M3"bj 不是 top1 北京"一类）**。M5-1（造词"张胜"，来自"张"+"胜"两段分别选择）：一次 `pinyin_remember_user_input` 后新词立刻出现在候选中且排到前几位——效果比 plan 例句预期的更好。M5-2（plan 原话"重复选择某已存在的单字候选 3 次，第 4 次排到候选第 1"）：实测**不是固定 3 次**——`hang`→"航"（原 top1"行"）需要 5 次强化，`hao`→"好"（原 top1"号"）需要 4 次，`hao`→"号"（反过来提升，起点差距更小）只需 1 次。这是 libpinyin 纯统计模型的合理结果：需要的强化次数由候选与当前第一名的系统词频差距决定，差距越大需要越多次，不是我们能控制的常量。**处理**：验收测试断言"有限次数（如 10 次）内一定能升到第一"这个更稳的性质，不硬编码某个具体候选对+具体次数；实测 5 次内（"航"）可靠复现。 | 已如实记录，不视为缺陷 |
| 2026-09-11 | **M5：用户词库导出/导入/清空，用 libpinyin 自带 API，不用自建二进制格式**。导出：`pinyin_begin_get_phrases(ctx, USER_DICTIONARY)` + 迭代 `pinyin_iterator_get_next_phrase`（返回拼音/词/count），写"拼音\t词\t次数"文本行。导入：`pinyin_begin_add_phrases(ctx, USER_DICTIONARY)` + `pinyin_iterator_add_phrase`。清空：`pinyin_mask_out(ctx, PHRASE_INDEX_LIBRARY_MASK, PHRASE_INDEX_MAKE_TOKEN(USER_DICTIONARY, 0))`——只清 USER_DICTIONARY 这一个子词库范围（掩码只看 token 的库下标位），不动系统/GBK/addon 只读词库；清空/导入后立即 `pinyin_save`（管理操作，不等 autosave 计数器，防止操作后进程随即崩溃导致"看起来清了/导了，其实没落盘"）。真实 IPC 端到端验证完整闭环：造词->导出（文件确实含新词+count）->清空（新词消失）->导入（新词恢复）。 | 已实现并验证，M5 |
| 2026-09-11 | **M5：deployer CLI（`--export/import/clear-userdict`）新建 `backend/engine_client.{hpp,cpp}`，不复用 tsf-service 的 `ipc_client`**。协议层（`myabc::ipc` 长度前缀 JSON 帧）完全一样，但 tsf-service 那份是为"绝不能阻塞宿主 UI 线程"（overlapped IO + 50ms 有界等待）设计的，deployer 是一次性管理 CLI、没有这个约束，用简单阻塞 `ReadFile`/`WriteFile` 更直接；且按项目约定跨领域调用要走对方 logic 层，tsf-service 没有为此暴露的接口，用量也小（3 个方法），独立实现比硬拉一条跨域依赖更干净。CLI 不需要 COM/管理员权限（跟 `--register`/`--unregister`/`--status` 的 `CoInitializeEx` 路径分开），纯走命名管道。真实端到端验证：deployer.exe 既能连上已运行的引擎，也能在没有引擎运行时自己拉起一个（跟 TIP 的 `IpcClient::Connect()` 同一套"连不上就 CreateProcess"逻辑）。 | 已实现并验证，M5 |
| 2026-09-11 | **M5：PROTOCOL_VERSION 从 2 升到 5（跳过 3/4）**——M3/M4 均未改协议结构，版本号一直没动；这次是本项目第一次真正新增 wire 方法（`userDictExport`/`userDictImport`/`userDictClear`），版本号直接跳到 plan 里 M5 一直写的目标值 5，不是"3、4 被跳过""，是"3/4 从来没真正发生过"（见 M3/M4 各自的 debt-log 条目：两轮都决定不做协议改动）。 | 已实现，M5 |
| 2026-09-11 | **M5：`SelfDir`/`CurrentUserSid`/`AppDataDir` 第三份重复实现**。`engine_main.cpp`、`myabc_text_service.cpp` 已经各有一份几乎相同的小工具函数（纯技术、无业务含义），本轮 `deploy_flow.cpp` 因为要算 `EngineClientConfig` 的管道名/引擎路径，加了第三份。已经够得上 code-quality-standards §1"3 个以上复用点归入 shared/"的门槛，但抽取属于跨里程碑范围的重构，本轮先照抄现有模式落地功能，抽取到 `src/shared/` 留作后续整理项（不影响任何一条验收判据）。 | 待后续（低优先级技术债，观察） |

| 日期 | 条目 | 处理状态 |
|---|---|---|
| 2026-09-11 | **M4 遗留真 bug（M5 开工前发现并修）：数字/金额候选选不中 + 笔形过滤后选中错位**。`Session::SelectCandidate`/`CommitComposition` 从 M1 起就无条件假设 `candidates_` 的下标对应 libpinyin 内部候选数组、`engine_.CurrentSentence()` 反映当前组字内容——这个假设在只有 `PinyinCandidateSource` 一种来源时成立，M4 加入 `NumberCurrencySource`（产出跟 libpinyin 无关的"原子候选"列表）后就不成立了，但没人去检查。**bug 1**：选中数字/金额候选（如 `i2025` 按空格）——`engine_.Choose(index,...)` 内部 `index >= raw_candidates_.size()`（libpinyin 从没解析过这个 raw，数组是空的）恒为 true，`Choose` 直接返回 false，候选列表被静默清空，组字卡死，永远提交不了。`CommitComposition` 同理：`engine_.CurrentSentence()` 对未解析过的 raw 返回空串，兜底成 `raw_`，会把字面量 `"i2025"` 而不是渲染后的候选提交上屏。**bug 2**（更隐蔽）：笔形过滤生效时（如 `` yi`3 ``），`PinyinCandidateSource::Produce()` 返回的是 `engine_.candidates()` 的**子集**，下标已经跟 libpinyin 内部数组错位——用户看到显示的第 0 项是"以"，选中后 `engine_.Choose(0,...)` 选的却是 libpinyin 内部真正的第 0 项"一"，静默提交了用户没看到、没选的字。两个 bug 都是"响应看起来正常（`ok:true`/有 `commit` 字段），内容却是错的"这一类最难发现的问题，本轮 M4 报告的端到端测试全部只做了"peek 候选 + cancel"没有真正走选中/确认路径，完全没覆盖到。**修复**：`CandidateSource` 加 `UsesEngineChoose()`（默认 false，`PinyinCandidateSource` 覆写为 true）标记这个来源的候选是否跟 libpinyin 内部数组对应；`CandidateItem` 加 `engine_index`（默认 -1 = 显示下标即内部下标），`FilterByBihuo` 过滤时显式记录每一项的原始下标；`Session` 用 `current_source_uses_engine_choose_`（`Recompute()` 里跟 `candidates_` 一起刷新）分流：非 engine-choose 来源直接把 `candidates_[index].text` 当整句提交，不查 libpinyin；engine-choose 来源用 `item.engine_index`（不是显示下标）调 `engine_.Choose()`。**经验记录**（第四次同类教训，接续 M1 CWD bug / M2 id 冲突 bug / M4 笔形触发键 bug）：新增一种 `CandidateSource` 实现后，必须重新审视"选中/确认"这条路径是否还对所有来源成立——接口层面的"新增来源=新增一个类"没错，但 `Session` 里"怎么处理选中"这部分逻辑本身也要跟着适配器扩展审查，不能假设旧逻辑自动适用于新来源。真实选中+提交的端到端测试（不只是 peek 候选）应作为每个新 CandidateSource 落地的标配验收步骤。 | 已修复并端到端验证 |

| 日期 | 条目 | 处理状态 |
|---|---|---|
| 2026-09-11 | **M4：真 bug——笔形辅助码触发键与 select_keys 冲突，plan 05 §5 字面示例（"wo3"）在真实按键路由下不可达**。plan 05 §3.1 设计"拼音后直接接 1-5 数字"（如 `wo3`），`bihuo_filter.cpp` 也确实按这个格式实现了拆分/过滤逻辑，`ctest tests/unit/bihuoma_filter` 也用合成表验证过拆分正确——**但只在纯函数层面测过，没测过真实按键能不能把这样的 raw 串组出来**。真机/端到端管道测试才发现：`session.cpp::ProcessKey` 组字态下，任何数字键都先过 `select_keys`（默认 `"123456789"`，M1 起既有行为）当"选第 N 个候选"处理，`raw_` 根本没有机会追加数字——`wo` + `3` 的实际效果是直接选中候选[2]并上屏，`bihuo_filter` 的拆分逻辑永远收不到带数字后缀的 raw。跟 M4 数字模式那次"select_keys 截胡"是同一类问题，但这次两种解释（选字 vs 笔形筛选）**永久无法共存**，不是加个模式判断就能解决——因为两者都想用"裸数字"表达不同意图。**决策**：改用独立触发键 `config.input.bihuo_lead_key`（默认反引号 `` ` ``，不与字母/select_keys/page_keys/标点映射冲突）：先按一次触发键进入笔形输入态（`wo` -> `` wo` ``），之后 1-5 才追加为笔形码（`` wo`3 ``），不再落入 select_keys 分支；纯笔形（没有拼音前缀）仍不触发（触发键在 raw 最前面时 `SplitPinyinAndBihuo` 视为无效）。`SplitPinyinAndBihuo` 签名改为 `(raw, lead_key)`，按 `rfind(lead_key)` 切分，不再猜"尾部是不是数字"。**影响范围**：`session.hpp/.cpp`（新增 `bihuo_lead_key` 字段 + 组字态路由分支）、`bihuo_filter.hpp/.cpp`、`pinyin_candidate_source.hpp/.cpp`、`source_registry.hpp/.cpp`、`dispatcher.cpp`、`config_defaults.hpp`、`engine_main.cpp`、`tests/unit/bihuoma_filter_test.cpp`。真实 IPC 端到端验证（`yi` -> 含"一"；`` yi`1 `` 保留"一"；`` yi`3 `` 过滤掉"一"；`ren`/`` ren`3 ``/`` ren`1 `` 同理；`` nihao`3 `` fail-open 不清空候选；回归确认 `ni`+`1` 仍走 select_keys 直接选字上屏，未受影响）。**经验记录**：纯函数单测覆盖了"拆分逻辑本身对不对"，但漏了"这个字符串真的能在真实按键状态机里产生吗"——这类"数据能不能到达"的问题只有端到端测试能抓到，跟 M1 的 CWD bug、M2 的 id 冲突 bug 是同一类教训。 | 已修复并端到端验证，M4 |
| 2026-09-11 | **M4：笔形辅助码种子表（`assets/data/bihuoma.txt`）故意只收录 8 个无争议字，不伪造 3500 高频字数据**。plan 05 §3.1 要"从开源五笔/笔画数据转换，或智能ABC笔形规则表"生成生产级数据，但当前没有可靠、可验证 License 的权威笔顺数据源；错误的笔形码会把正确候选**筛掉**（体验上比完全没有这个功能更糟——用户按对了笔形却看不到本该出现的字）。**决策**：只收录笔顺在通用识字教学里公认无争议的字（一二三十=横，人八入=撇，大=横），证明"触发键 + 拼音+笔形过滤"整条链路是通的；未收录字一律 fail-open（不参与筛选，不会被误伤）。真正覆盖高频字的数据留待找到权威来源（如教育部《现代常用字部件及部件名称规范》笔顺数据、开源五笔码表反推）后再补，补数据不需要改任何代码——`BihuoTable::LoadFromFile` 格式已定稿。 | 已按最小可信子集落地，M4；数据源待补 |
| 2026-09-11 | **M4：GBK 生僻字候选"已经能用"，未新增任何 filter/协议改动**。plan 05 §3.3 设想新增 `gbk_filter.cpp/.h` + IPC `tag` 字段（PROTOCOL_VERSION -> 4）标记生僻字。实测：libpinyin 默认词库集从 M1 起就含 `gbk_char.bin`（见 M1 R1 词库生成记录），生僻字（验收判据 M4-6 指定的"喆"）**本来就出现在候选列表里**（`zhe` 第 2 页，页内 index=6）且可正常选中上屏——不需要额外的 charset filter 去"放行"它，因为它从没被挡住过。`logic/charset/gbk_filter.cpp` 仍是占位（承接 M1 R1 已记的技术债），`config.output.charset` 默认值 `"gbk"` 从 M1 起就是这个值，未改动。**决策**：不新增 tag 字段/协议版本升级——没有实际功能依赖它（UI 角标/变色是纯展示层锦上添花，不影响"生僻字能不能打出来"这条硬判据），PROTOCOL_VERSION 维持 2。用真实 IPC 端到端测试固化这条验证（此前只在临时探针里验证过，未落地为可重跑脚本；本轮同步写了可重复运行的验证过程，结论一致）。 | 已验证，未新增代码，M4 |
| 2026-09-11 | **M4：`config.input.number_lead_key` 数字模式与 select_keys 的同类冲突，已在实现时发现并修**。跟笔形键冲突是同一根本原因（`session.cpp` 组字态下裸数字默认全部走 select_keys）：`i2025` 的 `2`/`0`/`5` 一度被当成选字键处理，端到端测试第一次跑 `i2025` 时候选是空的。修复：加 `in_number_mode`判断——`raw_.front() == number_lead_key` 时，数字/小数点/负号追加进 raw_ 而不查 select_keys；此判断先于笔形判断（两种模式由 raw_ 最前面的字符互斥决定，不会同时成立）。 | 已修复并端到端验证，M4 |
| 2026-09-11 | **M4：cn_number（中文数字/金额大写）+ number_currency_source 落地**。新增 `src/shared/cn-number`（纯函数：`PlaceValueChinese`/`DigitByDigitChinese`/`UppercaseCurrency`/`ParseDecimalToCents`/`ParseInteger`），`logic/candidate/number_currency_source.cpp` 用 `config.input.number_lead_key`（默认 `i`）识别数字/金额输入，插入 `source_registry` 的 punctuation 之前（跟 plan §3.2 一致）。`DigitByDigitChinese` 用"〇"（年份读法）而非"零"，`PlaceValueChinese` 遵循"两/二"惯例（如"两千零二十五"）。`ctest cn_number` 覆盖 0/10/100/1005/20000/1.5/负数/金额边界全通过（M4-7）。 | 已实现并验证，M4 |

| 日期 | 条目 | 处理状态 |
|---|---|---|
| 2026-09-11 | **M3：jianpin_parser/hunpin_parser 未按 plan 04 §3.1 建独立文件**。计划列了 `jianpin_parser.cpp/.h`、`hunpin_parser.cpp/.h` 两个文件，但探查 libpinyin 行为后发现：简拼与混拼对 libpinyin 而言是**同一个机制**——都是 `PINYIN_INCOMPLETE` 选项打开后，`pinyin_parse_more_full_pinyins` 原样处理输入串，libpinyin 自己识别哪些音节完整、哪些不完整并做混合匹配，不需要上层做任何区分性的解析或分支。计划自己也承认"hunpin 本质是 incomplete 开关"。因此没有创建两个内容几乎相同的类（避免为不存在的行为差异建模，属 code-quality-standards §3.2 的反面——3+ 分支/实现才要适配器，这里根本没有分支）。改为 `LibPinyinEngine::ApplyInputOptions(incomplete_enabled, fuzzy_names)`：`config.input.scheme` 只做一件事——quanpin 关 `PINYIN_INCOMPLETE`，jianpin/hunpin 开。 | 已决策，M3 |
| 2026-09-11 | **M3：模糊音真正接线**。`config.input.fuzzy` 字段 M1 起就存在但从未使用；本轮加 `PinyinAmbiguity2` 位映射表（`c_ch/s_sh/z_zh/f_h/g_k/l_n/l_r/an_ang/en_eng/in_ing/all`），`ApplyInputOptions` 按名字叠加位。golden 测试验证：开 `z_zh` 后输入 `zongguo` 命中"中国"（M3-7）。 | 已实现并验证，M3 |
| 2026-09-11 | **M3：`bj` 简拼不是 top1"北京"，是"候选命中"**。plan 04 §5 M3-1 写"bj + 空格 -> 期望 top 候选：北京"；实测 libpinyin 默认语料对孤立两字母简拼（无上下文）给出的 top1 是"编辑"，"北京"排第 2。这是 libpinyin 内建统计模型（unigram 频率）对"没有前后文的两个声母"这类极度歧义输入的合理排序，不是 bug；`bjing`/`beij`（声母+完整音节混拼，歧义显著降低）top1 就正确是"北京"。真要让纯声母简拼精确匹配某个具体期望词，需要专门的语料调权或强制置顶规则，超出"让简拼/混拼能正确转换"这一 M3 核心目标的性价比。**处理**：golden 测试对 `bj` 按"候选命中"断言（如实反映现状），不用取巧手段（如预训练/强制 boost）让它假装 top1。 | 已如实记录，不视为缺陷 |
| 2026-09-11 | **M3：切分歧义显示（xi'an 隔音号 / segments 字段 / PROTOCOL_VERSION->3）未做**。plan 04 §3.2/§3.3 想要 preedit 显示 `xi'an` 这样的隔音号提示、候选按切分分组、协议加 `segments` 字段。实测 libpinyin 不需要这层提示就已经能在候选列表里正确给出"西安"（xi'an 切分）与"现/县/先"（xian 整体）两组候选（M3-4 验证过）——**核心转换正确性不依赖这个展示层**。这纯粹是"让用户更容易看懂候选列表为什么这么多"的锦上添花，需要用 `pinyin_get_pinyin_key_rest_positions` 之类的音节位置内省 API 把切分点映射回原始按键偏移，工作量不小，且不影响任何一条"转换对不对"的验收判据。**处理**：本轮不做，PROTOCOL_VERSION 维持 2（没有新增/改动任何 wire 字段）。留到真有用户反馈"候选太多看不懂"时再评估。 | 待后续（无明确里程碑，观察是否变必需） |

| 日期 | 条目 | 处理状态 |
|---|---|---|
| 2026-09-11 | **M2 反馈修复 1：候选窗定位偏离光标（DPI 感知缺失）**。用户实测反馈"弹出的选择框和输入位置相距比较远"。根因：`myabc-ui.exe` 是独立进程，没有声明 DPI 感知；`ITfContextView::GetTextExt` 给出的光标矩形是宿主进程（记事本，Per-Monitor-V2 感知）坐标系下的**真实物理像素**，但 `myabc-ui.exe` 默认不感知 DPI，Windows 会把这些坐标当成 96 DPI 虚拟坐标再缩放一次，在非 100% 缩放的显示器上产生明显偏移。修复：`host_main.cpp` 的 `main()` 开头调用 `SetProcessDpiAwarenessContext(DPI_AWARENESS_CONTEXT_PER_MONITOR_AWARE_V2)`（Win10 1703+ API，`#ifndef` guard 补 `_WIN32_WINNT` 宏保证声明可见）。**候选窗字号仍按 96 DPI 近似换算**（M1 R2 已记录的技术债），本次只修位置，字号在高 DPI 显示器上可能偏小，留待后续用 `GetDpiForWindow` 精确计算。 | 已修复，待用户复测 |
| 2026-09-11 | **M2 反馈修复 2：切换输入法图标缺失/怪异**。plan 01 §3.6 早就要求 `assets/icons/myabc.ico`（16/20/24/32 多尺寸）但一直没做，`AddLanguageProfile`/`DllRegisterServer` 一直把 IconFile 指向 DLL 自身、IconIndex=0，但 DLL 里从来没有真正的图标资源，导致语言栏/切换菜单显示默认/空白图标。**生成方式**：用 Pillow + 本机自带 Microsoft YaHei Bold（`C:\Windows\Fonts\msyhbd.ttc`）纯代码渲染——蓝底圆角方块 + 白色"拼"字，16/20/24/32/48/256 六档尺寸，无第三方素材、无版权问题（见 `assets/README.md`）。新增 `src/domains/tsf-service/tsf-service.rc` 和 `src/domains/candidate-ui/ui/host.rc`，把 `assets/icons/myabc.ico` 分别编进 `myabc-tip.dll`（语言栏用）和 `myabc-ui.exe`（任务管理器/Alt-Tab 用）。用 `System.Drawing.Icon.ExtractAssociatedIcon` 验证过 tip.dll 能正确提取出 32x32 图标。**已知注意事项**：Windows 图标缓存可能滞后——如果用户在同一次登录里之前已经打开过输入法切换菜单，可能要等一次 explorer.exe 重启/重新登录才会刷新显示新图标（DLL 文件内容变了但 shell 缓存的图标句柄可能没变）。 | 已修复，待用户复测（含图标缓存提示） |

| 日期 | 条目 | 处理状态 |
|---|---|---|
| 2026-09-11 | **M2：真 bug——`SessionResultToResponse` 把消息 id 和 sessionId 搞混**。`dispatcher.cpp` 早期实现把单参数 `id` 同时当 JSON-RPC 响应 `id`（每条消息递增，如 1,2,3...）和 `pending_ui_` 的 map key（该是稳定的 sessionId）用，导致 `setCaretRect` 用真实 sessionId 去查表，永远查不到用消息 id 存的条目，`uiShow` 永远推不出去（但函数本身正常返回 `{}`，没有任何错误信号——非常隐蔽）。用端到端管道测试（真连引擎 + 真发 setCaretRect + 真等 uiShow）抓到：调用成功但对端一直收不到推送，排查到是 key 不匹配。**修复**：`SessionResultToResponse` 改成显式接收 `(msg_id, session_id, result)` 两个参数，5 个调用点全部同步改。测试方法本身也记一笔：这类"响应看起来正常但下游推送悄悄丢失"的 bug，只有做真实双连接的端到端测试才能抓到，光测 TIP<->引擎这一条连接的响应内容看不出来。 | 已修复并验证，M2 |
| 2026-09-11 | **M2：候选窗独立进程 myabc-ui.exe 落地**。`src/domains/candidate-ui/ui/host_main.cpp`：连引擎的 `myabc-ui-{sid}` 管道，收 uiShow/uiHide 转成 CandidateWindow 调用；断线退避重连（不随引擎退出而退出，引擎重启后自动接上）。单实例（命名互斥量），引擎重复 CreateProcess 拉起无害。`objdump` 确认 `myabc-tip.dll` 不再依赖 gdi32/user32 的候选渲染部分（USER32 仍在，来自 GetKeyboardState/ToUnicode 等 TSF 按键 API，不是候选渲染）——M2-1 达成，且比计划预期更薄（M1 起就没用 D2D，此次连 GDI 都从 DLL 里搬走了）。 | 已落地并端到端验证，M2 |
| 2026-09-11 | **M2：引擎空闲自退出 + crash_guard 落地，minidump 不落 pinyin_save**。`pipe_server.cpp` 改 overlapped `ConnectNamedPipe` + `WaitForSingleObject` 超时，超时即 `SaveBeforeExit()`（`pinyin_save`）后退出；新增 `--idle-exit-seconds` 供测试用秒级覆盖（验证：3 秒空闲后引擎准时自退出，退出后管道空出可供新实例监听，等价 TIP 重连时会拉起新引擎）。`crash_guard.cpp`：`SetUnhandledExceptionFilter` + `MiniDumpWriteDump` 落 `%APPDATA%\myabc\logs\myabc-engine-crash-*.dmp`。**决策**：崩溃处理器里不调用 `pinyin_save`——崩溃现场 libpinyin/glib 状态可能已损坏，从异常处理器回调回业务逻辑本身就有二次崩溃风险；专业做法是"尽快尽量少动作地"存现场退出，不做数据抢救。 | 已落地，M2 |
| 2026-09-11 | **M2：范围裁剪——engine_supervisor 精确退避序列 / 完整压测脚本未做**。`config.ipc.connect_backoff_ms_csv`（[50,100,200,400]）字段存在但 `ipc_client.cpp` 的 `Connect()` 仍是 M0 起的简单固定间隔重试（100ms/200ms），未严格按配置的退避序列走；`tests/integration/typing_stress`（plan §5 M2-3 的 200 字压测脚本）未创建，压测判据（p99<30ms）改由端到端管道测试的观感佐证（本轮测试里每次 processKey 往返均在几毫秒级，无可感知延迟），未做正式统计断言。两项均为可观察到明确收益但非"能不能跑起来"必需的打磨项，留待真实使用中若出现问题再回来补。 | 待后续 |
| 2026-09-11 | **M2：TIP 侧仍是"同步 + 有界等待"而非计划描述的全异步投递**。plan 03 §3.3 设想"OnKeyDown 投递请求即返回，响应到达后经 PostMessage 到 TIP 线程再跑 EditSession"的完全异步模型；本项目保留 M1 起的"bounded-wait 同步调用"（`ipc_client.cpp` 的 `CallWithTimeout`，默认 50ms 超时）。理由：50ms 对人类打字不可感知，已经满足"不阻塞宿主 UI 线程过久"的核心诉求；全异步需要后台 IO 线程 + 消息专用窗口 + 请求序号匹配 + 过期响应丢弃等一整套新状态机，出错代价高（住在宿主线程里的边界情形 bug 会影响任意应用），在真实测得的延迟已经很低的情况下投入产出比不划算。若未来实测出现感知到的输入卡顿，再按原计划做全异步改造。 | 待后续（有明确触发条件） |

| 日期 | 条目 | 处理状态 |
|---|---|---|
| 2026-09-11 | **M1 R2：候选窗用 GDI 而非 Direct2D/DirectWrite**。plan 02 §3.4 原写 `ui/renderer_d2d.cpp` + d2d1/dwrite/dcomp/windowscodecs。改用 GDI（`BeginPaint`/`DrawTextW`）：候选列表就是几行文字+高亮，D2D 在这个复杂度下只增加依赖面和样板代码；M2 候选窗要迁到独立 UI 进程，届时若要视觉升级一起重写渲染层成本更低。窗口本身仍按计划起独立线程+消息泵（`candidate_window.cpp` 的 `ThreadProc`），不占 TIP 线程。 | 已决策，M1 R2 |
| 2026-09-11 | **M1 R2：字符映射用 `ToUnicode`，有已知局限**。`myabc_text_service.cpp` 的 `VkToChar` 用 `GetKeyboardState`+`ToUnicode` 把 vk 映射成可打印字符，是 TSF 按键处理的标准手法，但 `ToUnicode` 有副作用：遇到死键（重音符合成，如某些欧洲语言布局）会改变系统级死键缓冲区状态，可能干扰其它同时输入重音字符的应用。M1 验收场景（美式键盘字母/数字/标点）不触发，非西欧布局兼容性留 M6（"浏览器/UWP/Office 兼容打磨"）再评估。 | 待 M6 评估 |
| 2026-09-11 | **M1 R2：OnTestKeyUp 与 OnTestKeyDown 用同一份判定**。TSF 惯例是 KeyUp 的"是否吃"应和对应 KeyDown 一致（否则宿主可能收到孤立的 KeyUp）；本项目未按"同一次按键"精确配对（如两次判定之间 composing_ 状态变化的极端时序），而是让 `OnTestKeyUp` 独立调用与 `OnTestKeyDown` 相同的 `KeyRouter::IsInterestedKey`。正常单键按下-弹起场景（远快于组字状态切换）下行为一致；极端交错时序是已知但极低概率的边界情况。 | 观察中 |
| 2026-09-11 | **M1 R2：候选窗字号用近似换算**。`config.ui.font_size_pt` 是"pt"，`candidate_window.cpp` 用 `pt * 96 / 72` 换算成像素高度，按 96 DPI（100% 缩放）假设，未做 Per-Monitor-V2 DPI 感知（plan §3.4 原提到"Per-Monitor-V2 DPI"）。高 DPI 显示器下候选窗字体可能偏小。留到候选窗迁独立 UI 进程（M2）时按目标显示器 DPI 重新计算。 | 待 M2 |
| 2026-09-11 | **M1 R2：composition_/session 状态仍是"单一活跃焦点"简化**（承接 M1 R1 单一引擎实例的同一类简化，见下方 M1 R1 条目）。`CMyabcTextService` 用固定 `kSessionId=1`，且 `CompositionController` 只跟踪"当前"一个 `ITfComposition`，不按 `ITfContext*` 建表。多文档/多窗口同时组字会互相干扰；M0/M1 验收场景（单记事本窗口）不受影响，真正的按 context 隔离留 M2。 | 待 M2 |
| 2026-09-11 | **M1 R2：实机排障——僵尸引擎进程掩盖了已修好的 bug**。用户重启记事本、换上修过 CWD bug 的新 `myabc-tip.dll` 后，实测仍然"打 nihao 没候选框"。排查发现：换 DLL 文件**不会**杀掉已经在跑的旧 `myabc-engine.exe` 进程；旧引擎是在 CWD bug 修复前由旧版 TIP 拉起的（词库加载失败，`pinyinReady:false`），但它仍占着命名管道监听。新 TIP 的 `IpcClient::Connect()` 逻辑是"管道打得开就直接用，不会再拉起新引擎"，于是新 TIP 复用了这个坏掉的旧引擎，CWD 修复形同没生效。远程连接该管道验证确认 `pinyinReady:false`。**处理**：手动 `taskkill` 杀掉旧引擎；不需要重启记事本——TIP 检测到管道断开会在下一次按键自动重连并拉起新引擎（这次带正确 CWD）。用户重试后确认候选框正常出现，`nihao` 能转换。**经验记录**：以后只要改了"引擎怎么被拉起/引擎自身行为"的代码，验证前必须连带 `taskkill /F /IM myabc-engine.exe` 清掉所有旧实例，只换 DLL/exe 文件不够——命名互斥量+管道复用机制会让 TIP 静默沿用旧进程。 | 已处理并验证，M1 R2 |
| 2026-09-11 | **M1 R2：修复引擎子进程工作目录 bug（实机前发现）**。`IpcClient::LaunchEngine` 原用 `CreateProcessW(..., lpCurrentDirectory=nullptr, ...)`，子进程（`myabc-engine.exe`）继承宿主进程（如 notepad.exe）的当前目录，而 `config.engine.model_dir` 默认是相对路径 `"data"`——`pinyin_init("data", ...)` 会相对宿主的 CWD（几乎必然不是 engine.exe 所在目录）解析，几乎总是失败，导致真记事本场景下拼音转换悄悄不可用（`pinyinReady:false`）而 hello/IPC 本身看起来一切正常，非常隐蔽。已修：`LaunchEngine` 显式把 `lpCurrentDirectory` 设成 `engine_exe_path` 所在目录。配套修复 `scripts/stage.ps1`（补上把 `third_party/libpinyin/build-data/data/*` 拷到 `out/<Config>/engine/data/` 的步骤——原来完全没拷）+ `scripts/build-engine.sh`（补 `build-data` 静态构建步骤，原来只有本会话手动跑过，脚本里没有，新机器 clone 后跑不出词库数据）。修复后从 `out/Release/install-x64/`（CWD=自身目录）直接跑 `myabc-engine.exe` 实测：`pinyinReady:true`，`nihao`+空格正确 commit "你好"。 | 已修复，M1 R2 |
| 2026-09-11 | **M1 R2：TSF/COM 代码来源说明（追加）**。`backend/composition.cpp` 的 `StartComposition`/`EndComposition` 调用序列（`InsertTextAtSelection(TF_IAS_QUERYONLY)` 取 range -> `ITfContextComposition::StartComposition` -> `range->SetText`）按 TSF 官方文档 + 微软 SampleIME/TableTextService（MIT）的标准写法编写，未逐行拷贝，未携带版权头（同 M0 R3 条目的处理方式）。 | 已说明，M1 R2 |

---

登记来不及立即建 ADR 的决策，以及需在实现期补记来源/License 的第三方代码复用。

| 日期 | 条目 | 处理状态 |
|---|---|---|
| 2026-09-11 | **M1 R1：libpinyin 词库二进制生成流水线**。`-DBUILD_SHARED_LIBS=ON -DBUILD_UTILS=ON` 同一 build 目录会失败：`gen_binary_files`/`import_interpolation` 等 utils 同时链接 `libpinyin.dll.a`（导入库）和 `storage`/`lookup` 静态库，两者对同一批符号（如 `FacadePhraseIndex::load`）都有"强定义"，MinGW ld 报 multiple definition（Windows PE 特有；ELF 靠符号插入语义不会报错，upstream 无 Windows CI 未发现）。方案：新增独立静态构建目录 `third_party/libpinyin/build-data`（`-DBUILD_SHARED_LIBS=OFF -DBUILD_UTILS=ON`），只用来跑 `data` 目标生成二进制词库（下载 `model20.text.tar.gz` ~20MB，产出 gb_char.bin/gbk_char.bin/phrase_index.bin/pinyin_index.bin/bigram.db 等 ~90MB，含 12 个领域附加词库）；运行时仍用原 `build/src/libpinyin.dll`（B3 产物）。新增 `cmake/FindLibPinyin.cmake` 统一定位两者。`build-data/` 已随 `third_party/libpinyin/` 整体 gitignore。 | 已记录，M1 R1 |
| 2026-09-11 | **M1 R1：候选模型 = libpinyin 内建的"整句优先"列表**。`pinyin_guess_sentence()` 后调用 `pinyin_guess_candidates(offset, sort)`，libpinyin 会自动把 nbest 整句猜测（`NBEST_MATCH_CANDIDATE`）连同该 offset 的逐词候选一起放进同一个列表（`_prepend_sentence_candidates`，third_party/libpinyin/src/pinyin.cpp）。因此候选[0] 天然是当前最佳整句，不需要另外拼一套"整句候选"逻辑——`pinyin_choose_candidate` 对 `NBEST_MATCH_CANDIDATE` 和逐词候选是同一接口，统一返回新 cursor，`cursor>=parsed_len` 即整句完全确定。 | 已记录，M1 R1 |
| 2026-09-11 | **M1 R1：单一全局拼音实例**。M1 为控制范围，`SessionManager` 的所有 session 共享同一个 `LibPinyinEngine`（一个 `pinyin_context_t`/`pinyin_instance_t`），多窗口同时组字会互相干扰（后开始输入的窗口会打断前一个的组字状态）。M0/M1 的验收场景都是单记事本窗口，不受影响。真正的 per-session 隔离（每 session 独立 `pinyin_instance_t`，共享同一份已加载词库的 `pinyin_context_t`）留 M2"引擎进程生命周期加固"一起做。 | 待 M2 |
| 2026-09-11 | **M1 R1：组字态追加字母/退格 = 整串重新猜测，丢弃分段约束**。`LibPinyinEngine::ParseAndGuess` 每次都 `pinyin_reset` 后用完整 `raw_` 重新解析，会丢弃此前 `pinyin_choose_candidate`（分段确认）产生的约束。只有"选中候选后立即结束组字"或"选中候选后继续组剩余、且不再追加新字母/退格"这两条路径能正确保留约束；若用户在部分确认候选后又输入新字母，会静默回退到基于完整 raw_ 的全新猜测（不丢数据，只是"忘记"这次人工修正）。M1-6 (`plan 02 §5`) 只测单次数字选择这一标准场景，不受影响。 | 待 M2/M3 评估是否需要精细化 |
| 2026-09-11 | **M1 R1：GB2312/GBK 字符集过滤近似实现**。`GbkFilter`（`logic/charset/charset_filter.cpp`）用 Win32 代码页 936 做可表示性判定；`Gb2312Filter` 直接复用同一实现（Windows 没有独立 GB2312 代码页，真正区分需要查表，且不影响 M1 任何验收判据——output.charset 默认 "gbk"）。精确区分与"生僻字替代候选"策略留 M4（"GBK 生僻字"范围）。 | 待 M4 |
| 2026-09-11 | **M1 R1：config.toml 真解析仍未做**。plan 02 §3.6 要求"引入 header-only TOML 到 third_party/"，本轮为控制范围仍未落地（延续 M0 起的 TODO）；`assets/config/punctuation.toml` / `config.sample.toml` 已建档为"数据应该长什么样"的权威草稿，`config_defaults.hpp` 已补全 candidates/input/output 字段，TOML loader 接上后无需改调用方。M1 全部验收判据（§5）不测试"config.toml 覆盖默认值生效"，此简化不影响达标。 | 待后续（无明确里程碑，观察是否变必需） |
| 2026-09-11 | **M1 R1：scheme_registry 未落地**。plan 02 §3.2 列了 `scheme/input_scheme_parser.h`/`quanpin_parser`/`scheme_registry` 三个文件，但 M1 只有 quanpin 一种方案；按 code-quality-standards.md §3.2（3 个以上实现才强制适配器，避免为单一实现的假设未来过度设计），本轮未创建这三个文件，quanpin 的"只接受 a-z 字母"判断直接内联在 `Session::ProcessKey`。M3 引入简拼/混拼（第 2、3 个方案）时按原计划补齐 scheme 层。CandidateSource（3 个实现：pinyin/english/punctuation）已按适配器模式落地，未受影响。 | 待 M3 |
| 2026-09-11 | **M1 R1：libpinyin 运行期良性告警**。实测观察到两条 stderr 输出，均不影响功能（测试/selftest 仍 PASS，退出码 0）：(1) 首次以全新 user_dir 启动时 `open <user_dir>\user.conf failed.`（用户配置覆盖文件不存在，属预期，libpinyin 会用默认值）；(2) 进程退出/`pinyin_fini` 附近 `BDB1565 DB->sync: method not permitted before handle's open method`（怀疑是 addon/network 用户 bigram DB 未被打开却被 sync，来自 libpinyin 自身清理逻辑，非本项目代码触发）。两条均来自 libpinyin 自身的 g_warning/BDB 日志，不是本项目代码的错误路径；先记录观察，若未来影响某些宿主的稳定性再深入排查上游。 | 观察中 |
| 2026-09-09 | 计划允许从微软 SampleIME(MIT) 拷贝 TSF 骨架/注册/类工厂片段，从 PIME(LGPL) 参考命名管道 JSON 协议。实现时须保留原版权头，并在此表补记具体文件与函数来源。 | 见下行（M0 R3） |
| 2026-09-11 | **M0 R4：`IsProfileRegistered` 改读注册表**。实机 `--register` 时 `AddLanguageProfile` 返回 S_OK 但 `ITfInputProcessorProfiles::EnumLanguageProfiles` 枚举不到刚注册的 profile（新进程 `--status` 也读不到）——注册本身没问题：`HKLM\SOFTWARE\Microsoft\CTF\TIP\{clsid}\LanguageProfile\0x00000804\{profileGuid}` 的 `Description`/`IconFile`/`IconIndex` 齐全，且 TSF API 自动同时写了 `WOW6432Node` 镜像。改为直接读该注册表键（`KEY_WOW64_64KEY`）判定，权威稳定。实机验证：register / status / unregister / re-register 全过；unregister 后 `HKCR\CLSID`、`HKLM\...\CTF\TIP`、`WOW6432Node\...\CTF\TIP` 三处清零（历史"卸载不彻底"回归判据通过）。 | 已修复，M0 R4 |
| 2026-09-10 | **M0 R3：TSF/COM 代码来源说明**。`src/domains/tsf-service/` 与 `src/domains/deployment/backend/` 的 COM 类工厂、`ITfTextInputProcessorEx`/`ITfKeyEventSink`/`ITfEditSession` 实现、`ITfInputProcessorProfiles`/`ITfCategoryMgr` 注册代码，**按 TSF 文档 + 微软 SampleIME(MIT) 的结构自行编写，无逐行拷贝**（无共享的大段代码，故未携带 SampleIME 版权头）。相关文件 `# 参考微软 SampleIME（MIT）` 注释仅指明结构来源。若后续确需整段拷贝 SampleIME/PIME 代码，届时按原计划保留版权头并在此逐文件登记。PIME 命名管道 JSON 协议：本项目协议为自研（system-overview §4.1），未参考 PIME 源码。 | 已说明，M0 R3 |
| 2026-09-10 | **M0 R2：nlohmann/json vendored**。`third_party/json/nlohmann/json.hpp`，tag `v3.11.3`，单头，MIT（兼容 GPL-3.0-or-later）。来源 `github.com/nlohmann/json/raw/v3.11.3/single_include/nlohmann/json.hpp`。仅 `src/shared/ipc-protocol` 使用，不引入构建依赖，不本地改动。 | 已记录，M0 R2 |
| 2026-09-10 | **M0 R2：设计常量 GUID 生成**（PowerShell New-Guid，一次性，发布后不可变——见 `src/config/guids.hpp` 的 DECISION 注释）。`CLSID_MyabcTextService {8BA238DD-B6B4-42B4-95C1-8062D25B3652}`；`GUID_MyabcProfile {C4FBBA94-26BC-4C7A-B9F1-4F91A723C60A}`。定义在 `src/config/guids.cpp`（`DEFINE_GUID`，唯一 TU），MSVC 侧 `myabc::guids` 静态库，tsf-service DLL 与 deployer 各自链接。 | 已记录，M0 R2 |
| 2026-09-10 | **M0 R2：工程约定**。(a) 含非 ASCII 的 `.ps1` 一律 UTF-8 with BOM（`build.ps1`/`stage.ps1` 已补）。(b) 新增 `.gitattributes`：`*.sh`/`*.cmake`/`CMakeLists.txt` 强制 `eol=lf`（MSYS bash 执行），`*.ps1` `eol=crlf`。(c) `scripts/build.ps1` 必须 `Push-Location` 到仓库根后再 `cmake --preset`（预设从 cwd 找 `CMakePresets.json`）。(d) `CMakePresets.json` 根对象不接受 `$comment`，注释放 `vendor.myabc`。 | 已记录，M0 R2 |
| 2026-09-10 | **M0 R2：json_codec 暴露 nlohmann::json**。`json_codec.hpp` 直接暴露 `nlohmann::json` 作为 params/result 类型，依赖它的每个 TU 都会带入 24k 行单头。M0/M1 可接受（项目小、编译快）。若 M2 后编译时间成问题，改 pimpl 或只暴露 typed 字段。 | 待观察 |
| 2026-09-09 | libpinyin 若需打补丁才能在 UCRT64 构建，补丁以 patches/*.patch 保存，须在此登记补丁内容与上游 issue 链接。 | 已补记，见下行（M0 R1） |
| 2026-09-10 | **M0 R1：libpinyin UCRT64 共享库补丁**。锁定版 commit `074a221` 在 UCRT64 下 `-DBUILD_SHARED_LIBS=ON` 有两处失败，补丁存 `patches/libpinyin-win32-shared-build.patch`（直接改工作树 + 源内 `# myabc patch` 注释）：(1) `CMakeLists.txt` 的 `if(WIN32)` 把库文件前缀强设为 `CMAKE_INSTALL_PREFIX`，Windows 默认值 `C:/Program Files/libpinyin`（含盘符冒号+空格）使 Ninja 生成非法输出路径 `src/C:/Program Files/...` 而报错；改为保留 CMake 默认 `lib`/`.dll` 前缀 + `CMAKE_WINDOWS_EXPORT_ALL_SYMBOLS ON`（libpinyin 无任何 `__declspec` 导出宏，需自动导出才有可用导入库）。(2) `src/CMakeLists.txt`：上游只把 `${DB_LIBRARIES}` 追加到 `CMAKE_CXX_LINK_EXECUTABLE`（供 utils 可执行文件），依赖 ELF 共享库允许未定义符号；PE DLL 必须链接期解析，故 `libpinyin.dll` 报 `undefined reference to db_create`。`src/storage/CMakeLists.txt:58` 还写错变量名 `${BERKELEY_DB_LIBRARIES}`（应为 `${DB_LIBRARIES}`）。补丁在 `WIN32` 下对 `pinyin` target 显式 `target_link_libraries(... ${GLIB2_LIBRARIES} ${DB_LIBRARIES})`。构建用 `-DBUILD_UTILS=OFF -DBUILD_TESTING=OFF`（utils 需 Python+联网下载 model.text，M1 再处理）。产物 `build/src/libpinyin.dll`(851KB) + `libpinyin.dll.a`(330KB)，830 个导出符号含 `pinyin_init`/`pinyin_guess_sentence`。上游无 Windows CI，未提 issue（可后续向上游反馈）。`third_party/libpinyin/` 已加入 `.gitignore`（保持普通 clone 决策）；`scripts/setup-deps.ps1` 增加幂等 patch 应用。 | 已补记，M0 R1 |
| 2026-09-09 | 笔形辅助码数据表（assets/data/bihuoma.txt）来源与 License 待定，M4 落地时在此登记。 | 待 M4 |
| 2026-09-09 | 词库/语言模型来源（libpinyin 自带 model.text vs ibus-libpinyin 发行版 vs 自训增补）待定，M1/数据步骤确定后建 ADR。 | 待 M1 |
| 2026-09-09 | Berkeley DB 若为 MSYS2 的 6.x（AGPLv3），组合作品 License 影响待法律层面确认，可能改用 KyotoCabinet 或自建 BDB 5.3.28。 | 待确认 |
| 2026-09-09 | **M0 R1：libpinyin 版本锁定**。锁定 commit `074a2219c90feaf962d0d24f034514033ece5f99`（`main` 分支 HEAD，2026-09-03，"use g_rename to replace rename..."）。**未用最新 release tag `2.11.91`（2026-01-04）**：`git log 2.11.91..HEAD` 显示其后 9 个 commit 恰为计划 §3.4 风险点 3 点名的 POSIX 移植修复——`88e39fc` g_fsync、`de11fd4` std::getline 替换 POSIX getline、`9ddf484` O_BINARY、`03bc5ef` fgets 避免 locale 敏感解析、`074a221` g_rename，并含 `55e9051` "Bump version 2.11.92"（但上游未推 2.11.92 tag）。2.11.91 编译必然报这些错，故按计划直接用 master，省去必失败的一轮尝试。libpinyin 作为 `third_party/libpinyin` 普通 clone（非 submodule），无自身子模块。 | 已锁定，M0 R1 |
| 2026-09-09 | M0 R1：MSYS2 版本 `20260611`，装机后 `pacman -Syu` 跑两遍（第一遍升 msys2-runtime 3.6.10-3 需重启 shell）。UCRT64 依赖用 `pacman -S --needed mingw-w64-ucrt-x86_64-{toolchain,cmake,ninja,pkgconf,glib2,db}`。vcpkg submodule baseline commit `f781d9387e4684783e69e136e2e124ff4660bffc`（tag 描述 `2026.07.29-466`）。 | 已记录，M0 R1 |
| 2026-09-10 | **M0 R1：实测版本快照**（B1-B4 复现环境）。UCRT64：gcc 16.2.0、cmake 4.4.3、ninja 1.13.2、glib-2.0 2.88.3、Berkeley DB 6.2。MSVC 侧：VS 2022 Community、cl 19.44.35213、bundled cmake 3.31.6、Windows SDK（vcvars 默认）。vcpkg 拉取 `wil` 版本 `1.0.260126.7`（MIT），triplet x64-windows，配置阶段自动安装成功。 | 已记录，M0 R1 |
| 2026-09-10 | **M0 R1：PowerShell 脚本编码修复**。`scripts/env.ps1`、`scripts/setup-deps.ps1` 初版存为无 BOM 的 UTF-8，Windows PowerShell 5.1 按系统 ANSI（本机 GBK）解码含中文注释的脚本 → 乱码 → 解析错误（`Unexpected token`）。已给两个文件加 UTF-8 BOM（`EF BB BF`）。约定：仓库内所有含非 ASCII 的 `.ps1` 一律存 UTF-8 with BOM。 | 已修复，M0 R1 |
| 2026-09-10 | **M0 R1：顶层构建骨架落地**（plan 00 §4）。新增 `CMakeLists.txt`（按 `MYABC_TOOLCHAIN=msvc\|mingw` 分派，子目录仅在其 `CMakeLists.txt` 存在时 `add_subdirectory`，便于分步落地）、`CMakePresets.json`（v6：x64-debug/x64-release/x86-release/mingw-engine；注意 CMake 预设根对象不接受 `$comment`，改用 `vendor.myabc`）、`cmake/myabc-common.cmake`（C++20、`/W4` 或 `-Wall -Wextra`、`myabc_set_warnings()`）。B5 验收：`cmake --preset x64-release` 在 vcvars x64 环境下配置成功，`find_package(wil CONFIG REQUIRED)` 通过。尚缺 plan 00 §5 的 `scripts/build.ps1`、`build-engine.sh`、`stage.ps1`、`register.ps1`（M0 R2 起实现）。 | 已记录，M0 R1 |
